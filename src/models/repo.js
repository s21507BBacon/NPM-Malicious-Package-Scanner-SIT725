class Repo {
    constructor(repoAddress, repoType, repoKey) {
      this.repoAddress = repoAddress;
      this.repoType = repoType;
      this.repoKey = repoKey;
    }
  }
  
  module.exports = Repo;